{
  "name": "webart",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "check": "npm run build"
  },
  "devDependencies": {
    "vite": "^8.0.3"
  }
}
